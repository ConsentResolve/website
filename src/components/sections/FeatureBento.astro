---
/**
 * Homepage feature section — visually mirrors the /features/ hub.
 *
 * Shape:
 *  - Section header (eyebrow Badge + double-line headline + subhead)
 *  - 4 group sub-blocks, each with a brand-soft Badge eyebrow, group
 *    title, blurb, and an IllustrationCard grid (same primitive used
 *    on /features/).
 *  - "See every feature" CTA at the bottom routes to /features/.
 *
 * Pulls every feature from src/data/features.ts so this stays in
 * lockstep with the dedicated /features/<slug>/ pages.
 */
import Badge from "~/components/ui/Badge.astro";
import IllustrationCard from "~/components/ui/IllustrationCard.astro";
import { FEATURE_GROUPS } from "~/data/features";
import { IconArrowRight } from "@tabler/icons-react";
---

<section class="section">
  <div class="container-content">
    <!-- Section header -->
    <div class="mx-auto mb-16 max-w-3xl text-center">
      <Badge tone="brand" variant="soft" size="md" uppercase>Everything it does</Badge>
      <h2 class="display-2 mt-5 text-[color:var(--color-ink)]">
        Every visitor who leaves without calling<br/>
        <span class="text-[color:var(--color-brand-pressed)]">is a job you'll never know you lost.</span>
      </h2>
      <p class="mt-5 text-lg leading-relaxed text-[color:var(--color-ink-2)]">
        Consent Resolve turns the anonymous people on your website into named leads you can actually call — exclusively yours, every one opted in.
      </p>
    </div>

    <!-- Groups -->
    <div class="space-y-24">
      {FEATURE_GROUPS.map((group) => (
        <div>
          <div class="mx-auto mb-10 max-w-3xl text-center">
            <Badge tone="brand" variant="soft" size="md" uppercase>{group.eyebrow}</Badge>
            <h3 class="font-display text-2xl font-black tracking-tight text-[color:var(--color-ink)] mt-5 md:text-3xl">
              {group.title}
            </h3>
            <p class="mt-3 text-[15px] leading-relaxed text-[color:var(--color-ink-2)]">{group.blurb}</p>
          </div>

          <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
            {group.features.map((f) => (
              <IllustrationCard
                illustration={f.illustration}
                title={f.name}
                body={f.howItHelps}
                href={`/features/${f.slug}/`}
              />
            ))}
          </div>
        </div>
      ))}
    </div>

    <div class="mt-16 text-center">
      <a
        href="/features/"
        class="inline-flex items-center gap-2 rounded-full bg-[color:var(--color-brand)] px-7 py-3.5 text-sm font-bold uppercase tracking-[0.14em] text-[color:var(--color-navy-900)] shadow-[0_18px_36px_-18px_rgba(0,229,160,0.55)] transition-all hover:-translate-y-0.5 hover:bg-[color:var(--color-brand-pressed)] hover:text-white"
      >
        See every feature
        <IconArrowRight size={16} stroke={2.5} />
      </a>
    </div>
  </div>
</section>
